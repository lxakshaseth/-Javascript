// // Initialize index with value 0
// let index = 0;

// // Run the loop while index is less than or equal to 10
// while (index <= 10) {

//     // Print the current value of index
//     console.log(`value of index is ${index}`);

//     // Increase index by 5
//     index = index + 5;
// }

// // Create an array containing superhero names
// let myArray = ['flash', 'batman', 'superman'];

// // Initialize arr with value 0 to access array elements
// let arr = 0;

// // Run the loop while arr is less than the array length
// while (arr < myArray.length) {

//     // Print the current array element
//     console.log(`value is ${myArray[arr]}`);

//     // Move to the next array index
//     arr = arr + 1;
// }   


//do while loop
// Initialize score with value 1
let score = 1;

// Start the do-while loop
do {
    // Print the current value of score
    console.log(`Score is ${score}`);

    // Increase score by 1
    score++;

// Continue the loop while score is less than or equal to 10
} while (score <= 10);